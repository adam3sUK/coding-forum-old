<div class="form-group-wrap">
  <%= form_with model: [ @topic, @topic.posts.build ] do |form| %>
    <div class="form-group mt-4">
      <%= form.text_area :body, :class => "form-control" %>
    </div>
    <div class="form-group">
      <%= form.submit "Reply", :class => "btn btn-primary mt-4" %>
    </div>
  <% end %>
</div>